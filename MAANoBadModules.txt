# MAANoBadModules - 白名单配置文件
# 每行填写一个 DLL 文件名，大小写不敏感。
# 被列出的 DLL 在 MAA 内部检测时会强制返回"未加载"，从而跳过弹窗。
# 以 '#' 开头的行是注释，空行会被忽略。
#
# 如果此文件为空（或不存在），白名单为空，MAA 的原始检测行为不受影响。
#
# MAA 默认检测以下四个 DLL（将它们加入白名单可屏蔽对应的弹窗）：
#   NahimicOSD.dll
#   AudioDevProps2.dll
#   GTII-OSD64.dll
#   GTIII-OSD64.dll
#
# ---- 在此处填写你想屏蔽的 DLL ----

